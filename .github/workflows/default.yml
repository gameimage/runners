# This is a basic workflow to help you get started with Actions

name: default
on:
  workflow_dispatch:
  # Build every 7 days
  schedule:
    - cron: "0 0 * * 0"

jobs:
  build:
    strategy:
      matrix:
        runner: [ linux, pcsx2, rpcs3, retroarch, wine ]
    runs-on: ubuntu-latest

    steps:
    - name: Checkout code
      uses: actions/checkout@v3

    - name: Create package
      run: |
        mkdir -p dist
        # For wine
        export BASE_CREATE=1
        export LAYER_CREATE=1
        # Install tools
        sudo apt install -y jq wget tar xz-utils pv git pcregrep
        # Custom packages
        mkdir -p bin
        export PATH="$(pwd)/bin:$PATH"
        wget -q --show-progress --progress=dot:binary -O bin/jq \
          https://github.com/jqlang/jq/releases/download/jq-1.7/jq-linux-amd64
        chmod +x ./bin/*
        # Build packages
        ( HOME="$(pwd)" FIM_DEBUG=1 ./${{ matrix.runner }}/build-arch.sh )
        # Move dist contents to release folder
        mv ./${{ matrix.runner }}/dist/* ./dist
        # Clear directories
        rm -rf ./${{ matrix.runner }}/dist
        rm -rf ./${{ matrix.runner }}/build

    - name: Set permissions for dist directory
      run: |
        sudo chown -R "$(id -u)":"$(id -g)" dist/
        sudo chmod -R 766 dist/

    - name: Upload artifact to release
      uses: actions/upload-artifact@v3
      with:
        name: artifacts_build
        path: dist

    - name: Compute Short SHA
      id: ssha
      run: |
        echo "ssha=$(git rev-parse --short HEAD)" >> $GITHUB_OUTPUT

    outputs:
      ssha: ${{ steps.ssha.outputs.ssha }}

  release:
      permissions: write-all
      needs: [build]
      runs-on: ubuntu-latest

      steps:
        - uses: actions/download-artifact@v3
          with:
            name: artifacts_build
            path: dist

        - name: release
          uses: marvinpinto/action-automatic-releases@latest
          with:
            title: continuous gameimage-1.4.x
            automatic_release_tag: gameimage-1.4.x
            prerelease: false
            draft: false
            files: |
              dist/*
            repo_token: ${{ secrets.GITHUB_TOKEN }}
